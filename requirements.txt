# ── 시스템 ───────────────────────────────────
tzdata>=2024.1          # zoneinfo 타임존 데이터 (Docker slim 이미지 대응)

# ── 데이터 수집 ───────────────────────────────
yfinance>=0.2.36
pykrx>=1.0.45
pandas>=2.0.0
numpy>=1.24.0
feedparser>=6.0.10
beautifulsoup4>=4.12.0
requests>=2.31.0

# ── AI 분석 ──────────────────────────────────
groq>=0.9.0

# ── FastAPI 백엔드 ────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
apscheduler>=3.10.0

# ── DB (TimescaleDB / PostgreSQL 비동기) ──────
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0

# ── Phase 3: Kafka 파이프라인 ─────────────────
confluent-kafka>=2.4.0

# ── Phase 4: Prometheus 계측 ──────────────────
prometheus-fastapi-instrumentator>=6.1.0
prometheus-client>=0.20.0

# ── Phase 6: 인증 (JWT + 비밀번호 해싱) ──────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
email-validator>=2.1.0
python-multipart>=0.0.9

# ── 보안: Rate Limiting ───────────────────────
slowapi>=0.1.9

# ── ML 예측 파이프라인 ────────────────────────────────────────────────────
lightgbm>=4.3.0          # 주가 방향성 예측 모델 (앙상블 1)
xgboost>=2.0.0           # 앙상블 2
catboost>=1.2.0          # 앙상블 3
scikit-learn>=1.4.0      # TimeSeriesSplit, metrics
shap>=0.45.0             # 피처 중요도 (설명 가능한 AI)
joblib>=1.3.0            # 모델 직렬화
optuna>=3.6.0            # 하이퍼파라미터 자동 튜닝

# ── 추가 데이터 수집 ──────────────────────────────────────────────────────
OpenDartReader>=0.2.3   # DART 재무제표 (선택, DART_API_KEY 필요)
pytrends>=4.9.2          # Google Trends 검색량
