apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ai-analyzer-hpa
  namespace: stock
  labels:
    app: ai-analyzer
    app.kubernetes.io/name: ai-analyzer
    app.kubernetes.io/component: analyzer
    app.kubernetes.io/part-of: stock-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ai-analyzer
  # worker 4GB × 3대 환경 — 최소 1개, 최대 2개
  minReplicas: 1
  maxReplicas: 2
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70  # CPU 사용률 70% 초과 시 스케일 아웃
  behavior:
    # 스케일 아웃: 적당히 (API 요금 제한 고려)
    scaleUp:
      stabilizationWindowSeconds: 120
      policies:
        - type: Pods
          value: 1
          periodSeconds: 120
    # 스케일 인: 천천히
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Pods
          value: 1
          periodSeconds: 180
