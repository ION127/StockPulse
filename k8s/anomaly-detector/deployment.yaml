apiVersion: apps/v1
kind: Deployment
metadata:
  name: anomaly-detector
  namespace: stock
  labels:
    app: anomaly-detector
    app.kubernetes.io/name: anomaly-detector
    app.kubernetes.io/component: detector
    app.kubernetes.io/part-of: stock-platform
spec:
  # Kafka Consumer 그룹 파티션 분산 처리 — 파티션 수 초과 시 의미 없음
  replicas: 1
  selector:
    matchLabels:
      app: anomaly-detector
  template:
    metadata:
      labels:
        app: anomaly-detector
        app.kubernetes.io/name: anomaly-detector
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: node-tier
                    operator: In
                    values:
                      - light

      # Harbor 프라이빗 레지스트리 인증
      imagePullSecrets:
        - name: harbor-pull-secret

      containers:
        - name: anomaly-detector
          # CI/CD 파이프라인에서 SHORT_SHA로 자동 업데이트됨
          image: 10.0.2.105/stock/anomaly-detector:89c9daa
          imagePullPolicy: Always

          # ConfigMap에서 공통 환경변수 주입 (임계값 설정 포함)
          envFrom:
            - configMapRef:
                name: stock-config

          # ─────────────────────────────────────────────
          # 헬스체크 설정
          # ─────────────────────────────────────────────
          livenessProbe:
            exec:
              command: ["sh", "-c", "kill -0 1"]
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3

          # ─────────────────────────────────────────────
          # 리소스 제한 설정 (이상 탐지 연산으로 CPU 사용 높음)
          # ─────────────────────────────────────────────
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
